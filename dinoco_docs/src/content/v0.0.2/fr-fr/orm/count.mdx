export const title = "count";

# count

Est utilisé pour compter des enregistrements.

---

## Ce que vous pouvez faire

- Filtrer avec `.cond(...)`
- Exécuter avec `.execute(&client)`

## Description des méthodes

- `.cond(...)` : restreint les enregistrements inclus dans le comptage.
- `.execute(&client)` : exécute le comptage dans la base de données.

## Retour

Le retour de `count` est :

```rust
DinocoResult<usize>
```

## Exemple de base

```rust
let total = dinoco::count::<User>()
    .execute(&client)
    .await?;
```

## Exemple avec filtre booléen

```rust
let total = dinoco::count::<User>()
    .cond(|w| w.active.eq(true))
    .execute(&client)
    .await?;
```

## Exemple avec filtre de texte

```rust
let total = dinoco::count::<User>()
    .cond(|w| w.name.includes("Ana"))
    .execute(&client)
    .await?;
```

## Prochaines étapes

- [**`find_many::<M>()`**](/v0.0.1/orm/find-many) : récupère des enregistrements dans une liste.
- [**`find_first::<M>()`**](/v0.0.1/orm/find-first) : récupère un seul enregistrement.
