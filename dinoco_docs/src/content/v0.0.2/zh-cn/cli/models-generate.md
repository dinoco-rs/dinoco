# 如何使用？

命令 `dinoco models generate` 从数据库中存储的最新迁移生成 Rust 模型。

它用于使生成的代码与已应用的模式结构保持一致。

---

## 命令功能

此命令会执行以下操作：

- 读取最新的可用迁移
- 重新构建生成的 Rust 模型
- 更新 Dinoco 类型化 API 使用的工件

## 何时使用

在以下情况下使用此命令：

- 您已应用迁移并希望重新生成代码
- 您需要在结构更改后更新模型
- 您希望确保类型化客户端反映最新状态

## 后续步骤

生成后，您现在可以在 Rust 代码中使用这些模型。
