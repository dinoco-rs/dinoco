export const title = 'insert_into';

# insert_into

Used to insert a record.

---

## What you can do

- `.values(item)`: defines the record to be inserted.
- `.returning::\<T\>()`: changes the return to a typed projection of the inserted item.
- `.execute(&client)`: executes the write to the database.

## Example with Extend #[insertable]

Use `#[insertable]` along with `#[derive(Extend)]` when you want `.values(...)` to accept a richer payload with new relationships and existing connections. Dinoco inserts the parent first and then processes everything recursively.

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(User)]
#[insertable]
struct UserWithPosts {
    id: i64,
    name: String,
    posts: Vec<PostWithComments>,
}

#[derive(Debug, Clone, dinoco::Extend)]
#[extend(Post)]
#[insertable]
struct PostWithComments {
    id: i64,
    title: String,
    published: bool,
    authorId: i64,
    comments: Vec<Comment>,
}

dinoco::insert_into::<User>()
    .values(UserWithPosts {
        id: 1,
        name: "Ana".to_string(),
        posts: vec![
            PostWithComments {
                id: 10,
                title: "Recursive Insert".to_string(),
                published: true,
                authorId: 0,
                comments: vec![Comment { id: 100, text: "first".to_string(), flagged: false, postId: 0 }],
            },
        ],
    })
    .execute(&client)
    .await?;
```

## Example with nested relationship

Use an `Extend` payload with `#[insertable]` to create the parent and also create the related item in the same flow.

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(User)]
#[insertable]
struct UserWithProfile {
    id: String,
    username: String,
    role: Role,
    profile: Option<Profile>,
}

dinoco::insert_into::<User>()
    .values(UserWithProfile {
        id: "user-1".to_string(),
        username: "bia".to_string(),
        role: Role::USER,
        profile: Some(Profile {
            id: "profile-1".to_string(),
            bio: Some("Software Engineer".to_string()),
            userId: String::new(),
        }),
    })
    .execute(&client)
    .await?;
```

## Example with nested connection

When the relationship points to existing records, the codegen generates an enum like `ArticleConnection` to be used within the payload.

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(Article)]
#[insertable]
struct ArticleWithLabels {
    id: String,
    title: String,
    labels: Vec<ArticleConnection>,
}

dinoco::insert_into::<Article>()
    .values(ArticleWithLabels {
        id: "article-10".into(),
        title: "Connect Existing".into(),
        labels: vec![ArticleConnection::Label("label-10".into())],
    })
    .execute(&client)
    .await?;
```

## Example with ArticleConnection

This is the most common format for connecting existing records in many-to-many relationships.

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(Article)]
#[insertable]
struct ArticleWithLabels {
    id: String,
    title: String,
    labels: Vec<ArticleConnection>,
}

let value = ArticleWithLabels {
    id: "article-20".into(),
    title: "Connect Existing".into(),
    labels: vec![
        ArticleConnection::Label("label-10".into()),
        ArticleConnection::Label("label-11".into()),
    ],
};

dinoco::insert_into::<Article>()
    .values(value)
    .execute(&client)
    .await?;
```

## Return

Without `.returning::\<T\>()`, the return is:

```rust
DinocoResult<()>
```

With `.returning::\<T\>()`, the return becomes:

```rust
DinocoResult<T>
```

## Simple example

```rust
dinoco::insert_into::<User>()
    .values(User {
        id: "usr_1".to_string(),
        email: "ana@acme.com".to_string(),
        name: "Ana".to_string(),
    })
    .execute(&client)
    .await?;
```

## Example with typed return

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(User)]
struct UserSummary {
    id: i64,
    name: String,
}

let created = dinoco::insert_into::<User>()
    .values(User { id: 1, name: "Matheus".to_string() })
    .returning::<UserSummary>()
    .execute(&client)
    .await?;
```

## Example with worker

```rust
use database::*;

let _worker = workers()
    .on::<User, _, _>("user.created", |job| async move {
        println!("User created: {}", job.data.name);
        job.success();
    })
    .run()
    .await?;

dinoco::insert_into::<User>()
    .values(User { id: 1, name: "Matheus".to_string() })
    .enqueue("user.created")
    .execute(&client)
    .await?;
```

Learn more about workers in [**`queues`**](/v0.0.2/orm/queues).

## Next steps

- [**Derives**](/v0.0.2/core/derives): understand when to use `Rowable`, `Extend`, and `#[insertable]`.
- [**`insert_many::\<M\>()`**](/v0.0.2/orm/insert-many): batch insertion.
- [**`update::\<M\>()`**](/v0.0.2/orm/update): record update.
