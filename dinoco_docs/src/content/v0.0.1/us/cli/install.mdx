# Install

Install the CLI to bootstrap projects, generate artifacts, and validate your local environment.

## Init

```bash
cargo install dinoco
dinoco init
```

## Configuration

- Set your environment variables.
- Define the database provider.
- Generate the initial workspace files.

## Verification

Run `dinoco doctor` to validate dependencies and local setup.
