export const title = "delete";

# delete

明示的なフィルターを使用して削除するために使用されます。

---

## できること

- `.cond(...)`: どのレコードを削除するかを定義します。
- `.execute(&client)`: データベースで削除を実行します。

## 戻り値

`delete` の戻り値は次のとおりです。

```rust
DinocoResult<()>
```

## 基本例

```rust
dinoco::delete::<User>()
    .cond(|w| w.id.eq(10))
    .execute(&client)
    .await?;
```

## 別のフィルターの例

```rust
dinoco::delete::<Session>()
    .cond(|w| w.token.eq("session-1"))
    .execute(&client)
    .await?;
```

## 次のステップ

- [**`delete_many::<M>()`**](/v0.0.1/orm/delete-many): 一括削除。
- [**`find_many::<M>()`**](/v0.0.1/orm/find-many): 削除前にレコードを検証します。
