export const title = "find_many";

# find_many

レコードのリストを取得するために使用されます。

---

## 可能な操作

- `.select::<T>()` を使用してプロジェクションを選択する
- `.cond(...)` を使用してフィルタリングする
- `.take(...)` を使用して制限する
- `.skip(...)` を使用してページネーションする
- `.order_by(...)` を使用してソートする
- `.includes(...)` を使用してリレーションをロードする
- `.count(...)` を使用してリレーションをカウントする
- `.read_in_primary()` を使用してプライマリデータベースサーバーでの読み取り操作を強制する
- `.execute(&client)` を使用して実行する

## メソッドの説明

- `.select::<T>()`: モデルのデフォルトのプロジェクションをカスタムプロジェクションに置き換えます。
- `.cond(...)`: クエリにフィルター条件を追加します。
- `.take(...)`: 返されるレコードの数を制限します。
- `.skip(...)`: 結果が返される前に、指定された数のレコードをスキップします。
- `.order_by(...)`: クエリのソート順を定義します。
- `.includes(...)`: メインレコードと一緒にリレーションをロードします。
- `.count(...)`: リレーションのカウントを計算し、`posts_count` のようなフィールドを埋めます。
- `.read_in_primary()`: レプリカを使用せずに、プライマリデータベースサーバーでの読み取り操作を強制します。
- `.execute(&client)`: データベースでクエリを実行します。

## 戻り値

`select::<T>()` なしの場合、戻り値は次のとおりです。

```rust
DinocoResult<Vec<M>>
```

`select::<T>()` ありの場合、戻り値は次のようになります。

```rust
DinocoResult<Vec<T>>
```

## 基本例

```rust
let users = dinoco::find_many::<User>()
    .execute(&client)
    .await?;
```

## フィルター付きの例

```rust
let users = dinoco::find_many::<User>()
    .cond(|w| w.email.eq("ana@acme.com"))
    .execute(&client)
    .await?;
```

## ページネーションと並び替えの例

```rust
let users = dinoco::find_many::<User>()
    .order_by(|w| w.name.asc())
    .skip(20)
    .take(10)
    .execute(&client)
    .await?;
```

## カスタム select の例

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(User)]
struct UserListItem {
    id: i64,
    name: String,
}

let users = dinoco::find_many::<User>()
    .select::<UserListItem>()
    .execute(&client)
    .await?;
```

## 単純な include の例

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(User)]
struct UserWithPosts {
    id: i64,
    name: String,
    posts: Vec<Post>,
}

let users = dinoco::find_many::<User>()
    .select::<UserWithPosts>()
    .includes(|i| i.posts())
    .execute(&client)
    .await?;
```

## フィルター付き include の例

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(User)]
struct UserWithPublishedPosts {
    id: i64,
    name: String,
    posts: Vec<Post>,
}

let users = dinoco::find_many::<User>()
    .select::<UserWithPublishedPosts>()
    .includes(|i| i.posts().cond(|w| w.published.eq(true)))
    .execute(&client)
    .await?;
```

## ネストした include の例

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(Comment)]
struct CommentListItem {
    id: i64,
    text: String,
}

#[derive(Debug, Clone, dinoco::Extend)]
#[extend(Post)]
struct PostWithComments {
    id: i64,
    title: String,
    comments: Vec<CommentListItem>,
    comments_count: usize,
}

#[derive(Debug, Clone, dinoco::Extend)]
#[extend(User)]
struct UserWithPosts {
    id: i64,
    name: String,
    posts: Vec<PostWithComments>,
}

let users = dinoco::find_many::<User>()
    .select::<UserWithPosts>()
    .includes(|i| {
        i.posts()
            .includes(|post| post.comments().take(3))
            .count(|post| post.comments())
    })
    .execute(&client)
    .await?;
```

## リレーション数の例

```rust
#[derive(Debug, Clone, dinoco::Extend)]
#[extend(User)]
struct UserWithPostsCount {
    id: i64,
    name: String,
    posts_count: usize,
}

let users = dinoco::find_many::<User>()
    .select::<UserWithPostsCount>()
    .count(|i| i.posts())
    .execute(&client)
    .await?;
```

## プライマリデータベースから読み取る例

```rust
let users = dinoco::find_many::<User>()
    .read_in_primary()
    .take(5)
    .execute(&client)
    .await?;
```

## 次のステップ

- [**`find_first::<M>()`**](/v0.0.1/orm/find-first): 最大1つのレコードを取得するためのバージョン。
- [**`count::<M>()`**](/v0.0.1/orm/count): フィルター付きのレコードのカウント。
