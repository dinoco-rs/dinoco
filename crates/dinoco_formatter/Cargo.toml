[package]
name = "dinoco_formatter"
version = "1.0.0"
edition = "2024"
description = "A formatter for keeping Dinoco schema files clean, stable, and canonical."

[dependencies]
dinoco_compiler = { path = "../dinoco_compiler", version = "1.0.0" }
