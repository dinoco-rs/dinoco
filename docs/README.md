# Dinoco documentation

Documentation website for Dinoco. The English and Brazilian Portuguese content lives under `src/content/v1.3.3`; the site currently exposes this version only.

```bash
yarn install
yarn dev
```

Run `yarn check` before publishing changes. It validates every content route and heading, type-checks the application, and creates a production build.
